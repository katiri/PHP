<?php
    // É como se fosse um if/else resumido
    // Estrutura:
    // (comparação(ões)) ? Valor se verdadeiro : Valor se falso
    $a = 5;
    $b = 2;
    echo $a > $b ? "$a é maior que $b <br>" : "$a não é maior que $b <br>";

    $a = 5;
    $b = 7;
    echo $a > $b ? "$a é maior que $b <br>" : "$a não é maior que $b <br>";