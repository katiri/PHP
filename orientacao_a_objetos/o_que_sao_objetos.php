<?php
    // Objetos são basicamente entidades que possuem comportamentos e características
    // Em PHP objetos são definidos por classes, seus comportamentos são funções próprias e suas caracteríticas, variáveis próprias
    // Essas funções também podem ser chamadas de métodos e as variáveis podem ser chamadas de propriedades
    // As classes contém o molde do objetos, com seus métodos e propriedades iniciais
    // Os valores das propriedades podem ser mudados ao invocar um novo objeto daquela classe, mas esses valores partem do que a classe impõe

    // Declarando uma classe
    class User{
        // Aqui serão declaradas as propriedades e métodos
    }