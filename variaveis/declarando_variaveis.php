<?php
    $texto = 'Texto qualquer';
    $numero = 5; // Número qualquer
    $_variavel = 0.3;

    $somaDeVariaveis = $numero + $_variavel;

    $nomeVariaveis = 'Toda variável deve começar com $ seguida de seu nome, o formato mais recomendável para o nome de uma variável 
    é o nome sem carcteres especiais, no caso de mais de uma palavra, começar com primeira palavra toda minuscula e a primeira letra 
    das outras palavras em maiúsculo, como o nome dessa variável = $nomeVariaveis';

    echo $nomeVariaveis;

    